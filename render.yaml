services:
  - type: web
    name: RoamSafe
    env: java
    region: frankfurt
    branch: main
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/roamSafe-1.0.0.jar
    plan: free
    autoDeploy: true
